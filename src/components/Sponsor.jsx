import React from 'react'

const Sponsor = ({image}) => {
  return (
    <div class="slide">
      <img
        src={image}
        height="100"
        width="250"
        alt=""
      />
    </div>
  )
}

export default Sponsor
